import styled from 'styled-components';
import { motion } from 'framer-motion';

export const Proyect = styled(motion.div)`
    max-width: 1200px;
    margin: auto;
    padding:20px
    transition:1s;
`;